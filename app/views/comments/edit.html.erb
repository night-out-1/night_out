<h2>Edit your comment:</h2>

<%= form_for @comment do |f| %>

	<%= f.label :title, value: "Title: " %>
	<%= f.text_field :title %><br>

	<%= f.label :content, value: "Content: " %>
	<%= f.text_field :content %><br>

	<%= f.submit "Update Comment" %>

<% end %>